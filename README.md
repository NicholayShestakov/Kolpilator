### Соглашение о вызовах
Аргументы передаются в a0-a7 регистрах.  
Возвращаемое значение кладётся в регистр a0.
### TODO
- Продумать получше возврат значений (есть цепочка выражений, надо вернуть последнее значение последнего из них)
- Переделать сохранение окружения при вызове функции (сделать, чтобы сохраняла вызываемая функция, а не вызывающая)
- Сделать возможность в функции и let делать несколько выражений
- Переделать мэтчинг на более адекватный (сделать возврат окружения для получше возврата значений)
